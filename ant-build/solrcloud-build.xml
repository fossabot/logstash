<project name="Solr Cloud Logstash">
    <description>
        Jenkins Ant file for Solr Cloud Logstash
    </description>
 <property name="trunk.dir" value="${basedir}"/>
 <basename property="app.name" file="${basedir}"/>
 <property name="app.dir" value="${dest.dir}/${app.name}"/>
 <property environment="env"/>
 
 <target name="copy-configuration">
	<copy todir="${dest.dir}" overwrite="true" force="true" >
        <resources>
            <file file="${env.WORKSPACE}/configuration/solr/solr.conf"/>
        </resources>
	</copy>
    <copy 
        file="${env.WORKSPACE}/configuration/solr/solr.conf" 
        tofile="${dest.dir}/solr.conf" 
        overwrite="true"
    />
    
 </target>
 <!--
 <target name="restart-logstash">
    <exec failonerror="true" dir="${dest.dir}" executable="sudo">
        <arg line="/etc/init.d/logstash restart"/>
    </exec>
 </target>
 -->
</project>