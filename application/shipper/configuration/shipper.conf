# The # character at the beginning of a line indicates a comment. Use
# comments to describe your configuration.
input {
    beats {
        port => 5044
    }
}
output {
    if [application_source] == "solr.solr" or [application_source] == "solr.tomcat" {
        s3 {
            access_key_id => "access_key_id_value"
            secret_access_key => "secret_access_key_value"
            region => "region_value"
            bucket => "bucket_value"
            prefix => "prefix_solrcloud_value"
            size_file => 1073741824
            time_file => 5
            codec => "json_lines"
        }
    } else if [application_source] == "myjsdb" {
        s3 {
            access_key_id => "access_key_id_value"
            secret_access_key => "secret_access_key_value"
            region => "region_value"
            bucket => "bucket_value"
            prefix => "prefix_solrcloud_value"
            size_file => 1073741824
            time_file => 5
            codec => "json_lines"
        }
    } else {
        s3 {
            access_key_id => "access_key_id_value"
            secret_access_key => "secret_access_key_value"
            region => "region_value"
            bucket => "bucket_value"
            prefix => "logstash_junk/"
            size_file => 1073741824
            time_file => 5
            codec => "json_lines"
        }
    }
}